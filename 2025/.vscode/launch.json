{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Run jX/part_X (fixed cwd)",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "cwd": "${fileDirname}",
            "console": "integratedTerminal"
        }
    ]
}